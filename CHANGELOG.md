# Changelog

這份文件用來簡單記錄每個版本做了什麼。格式建議維持：

```text
## v版本號 - YYYY-MM-DD

- 變更 1
- 變更 2
- 修正 1
```

## v1.7 - 2026-08-01

- 下載歌曲頁新增「最新 / 熱門」排序，熱門模式會一次載入完整頻道清單並依觀看數排序。
- 新增 `video_stats` SQL 快取 YouTube 觀看數，畫面會先顯示 SQL 既有數字，過期資料改由背景更新。
- 背景更新觀看數顯示 `目前/總數（百分比）` 進度，成功一筆即寫入 SQL。
- 觀看數更新失敗會記錄冷卻時間，會員限定等永久不可抓影片會以 `view_count = -1` 略過後續更新。
- yt-dlp 共用設定新增 cookies.txt 與 Deno runtime 支援，改善 YouTube bot 驗證與 signature solving 問題。
- 下載歌曲頁改為每頁 4 首，已下載歌曲顯示灰色停用勾選，可下載歌曲勾選維持藍色。
- 下載歌曲頁支援跨頁勾選並顯示已選歌曲數，切換歌手或排序會清空選取。
- 批次下載按鈕移到工具列最右側。
- 修正切回音樂播放器時重載播放清單導致播放狀態被重置的問題。

## v1.6.1 - 2026-07-31

- 播放器設定頁新增播放順序設定，可選照順序或隨機。
- 隨機模式支援相同機率與評分權重。
- 評分權重抽籤公式為 `歌曲演算法分數 + 歌手演算法分數 / 2 + random(1~5)`。
- `random(1~5)` 會在每次抽下一首時，對每首候選歌曲重新產生一次。
- 新增同曲重複間隔設定，避免同一首歌太快再次出現。
- 播放器設定頁新增「開始」與「儲存設定」兩種套用方式。
- 播放資訊改為歌名在上、歌手在下，歌名使用粗體。
- 修正上一首/下一首顯示與按鈕狀態，上一首為空時停用按鈕。

## v1.6.0 - 2026-07-30

- 新增歌曲評分與歌手評分資料表，評分紀錄每次送出都保留為單筆資料。
- 評分支援 `0-10` 分，預設為 `5` 分，並可選擇「影響演算法」或「單純評分」。
- 新增評分演算法分數：最新 5 筆影響演算法評分權重為 `5, 3, 2, 1, 1`，全部平均權重為 `3`。
- 歌曲編輯頁新增歌曲評分，歌手編輯頁新增歌手評分。
- 播放器右側新增目前歌曲與該歌曲歌手的評分區。
- 歌曲/歌手編輯頁同一天同一目標只能評一次；播放器同日可多次評，但同一次播放只能送一次。

## v1.5.3 - 2026-07-30

- 歌曲管理清單改為分頁顯示，每頁 5 首。
- 歌曲管理清單移除內部捲軸，調整列高與間距。
- 歌曲管理新增搜尋頁入口。
- 搜尋頁支援用歌曲名稱、歌手與歌曲標籤篩選。
- 搜尋結果沿用歌曲清單分頁顯示。

## v1.5.2 - 2026-07-30

- 新增「單曲下載」頁，可輸入 YouTube 影片網址解析單曲資訊。
- 單曲下載會依影片 channel ID 自動比對既有歌手，找不到時必須手動選擇歌手。
- 單曲下載會判斷是否已下載，已存在 MP3 時停用下載避免重複下載。
- 單曲解析後會清空輸入欄並顯示預覽縮圖。
- 設定頁新增 cache 空間大小顯示與清空 cache 按鈕。

## v1.5.1 - 2026-07-16

- 新增音樂播放器首頁，使用 VLC 播放已下載 MP3。
- 播放器支援播放/暫停、上一首、下一首與拖曳時間條跳轉。
- 播放器顯示上一首、本首、下一首，並加入封面圖、歌名、歌手、音量拉條。
- 音量設定會保存到本機設定檔，下次啟動沿用上次音量。
- 新增播放器「標籤偏好」分頁，先預留篩選與加權標籤區域。
- 新增右側評分區預留欄位。
- 已下載歌曲封面改存最高畫質 `maxresdefault`，預覽列表仍用低畫質縮圖。
- 新增設定頁，顯示歌曲總空間與圖片總空間。
- 下載歌曲頁新增底部進度欄，下載進度顯示為 `目前/總數`。
- 修正下載歌曲頁切頁後捲軸未回到最上方的問題。
- 修正下載歌曲頁預覽圖比例被壓縮的問題。

## v1.4.2 - 2026-07-16

- 新增歌曲標籤 SQL 與管理頁，預設上層分類為類型、語言、其他。
- 歌曲編輯改為獨立編輯頁，支援修改歌曲名稱與勾選歌曲標籤。
- 歌曲清單簡化為歌手名稱、歌曲名稱、歌曲長度與編輯按鈕。
- 歌曲編輯頁顯示原始 YouTube 標題、檔案位置、歌曲長度、上傳日期與狀態。
- 修正歌曲清單編輯按鈕被下方文字蓋住的版面問題。
- 修正歌手清單頭像載入，優先從本機 assets 讀取。
- 下載歌曲區搜尋改為按下搜尋按鈕或 Enter 後才套用，避免輸入時列表閃爍。

## v1.4.1 - 2026-07-15

- 調整歌手管理 UI：歌手清單改為第一個分頁，新增歌手改為第二個分頁。
- 歌手清單改為三欄固定大小卡片，僅顯示歌手名稱與 artist_id。
- 新增標籤管理 UI，支援上層與下層標籤新增、停用、恢復與刪除。
- 標籤管理新增排序功能，上層與下層標籤可用上下按鈕調整順序。
- 上層標籤選擇改為名稱前方的勾選框。
- 歌手編輯頁支援多選歌手標籤。
- 歌曲管理與歌手管理圖片改用持久化 assets 目錄，並支援從舊 cache 補回圖片。
- 歌曲管理頁優先同步讀取本機縮圖，減少顯示 No Image 的情況。
- 下載歌曲時會保存並更新歌曲長度與上傳日期，供後續檢視與篩選使用。
- 全站字體與主要按鈕尺寸調整，改善高解析度與全螢幕使用體驗。
